export enum ExpenseTypeEnum {
  expense = 'expense',
  bonus = 'bonus',
}

export enum ExpenseIDEnum {
  expense = 'idExpenses',
  bonus = 'idBonus',
}

export enum CollectionIDEnum {
  expense = 'id_expense',
  bonus = 'id_bonus',
}

export enum ExpenseChangedEnum {
  expense = 'expenseChanged',
  bonus = 'bonusChanged',
}
